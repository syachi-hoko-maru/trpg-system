export const basicSize = 60;
export const maxX = 15;
export const maxY = 11;
export const width = basicSize * maxX;
export const height = basicSize * maxY;
export const centerX = (maxX - 1) / 2 + 1;
export const centerY = (maxY - 1) / 2 + 1;
