/** 画像フォルダ（PNG/JPG） */
export const imageDir = `${process.cwd()}/public/image`;
/** webpフォルダ */
export const webpDir = `${process.cwd()}/public/webp`;

/** blog.json path */
export const blogJSON = `${process.cwd()}/assets/blog.json`;
/** scenario.json path */
export const scenarioJson = `${process.cwd()}/assets/scenario.json`;

/** blog/scenario text filesフォルダ */
export const blogTextDir = `${process.cwd()}/text`;
/** blog画像フォルダ（PNG） */
export const blogImgDir = `${imageDir}/blog-image`;
